# Stage 5 Reality Audit: Clinical SLM Engineering & Training Infrastructure

**Date of Audit**: 2026-09-09  
**Auditor**: SLM Engineer  
**Target Subsystem**: `stage-4-slm/slm-engineer/`  
**Base Architecture**: `Qwen/Qwen2.5-1.5B-Instruct` (1,562,179,072 parameters)  
**Tokenizer**: `Qwen2TokenizerFast` (151,665 vocabulary tokens)  
**PEFT LoRA Configuration**: $r=16, \alpha=32$ on 7 projection targets across 28 layers (18,464,768 trainable parameters, 1.1820%)  
**Dataset Lineage**: `stage-4-slm/data-engineer/data/slm_finetune_dataset_v1.parquet` (SHA-256: `95d684c0940...`)  
**Hardware Environment**: Windows 10 AMD64 (CPU Only, PyTorch `2.14.0+cpu`, CUDA `False`)  

---

## 1. Executive Reality Audit Verdict

### Formal Status: `NOT VERIFIED — REAL TRAINING REQUIRED`

> [!CAUTION]
> **RULE 1 AUDIT COMPLIANCE & HONEST DISCLOSURE**  
> While the codebase, tokenizer, dataset collator, PEFT LoRA target layer definitions, ChatML formatting, and 18 unit tests are 100% genuine and production-ready, **real GPU training of the 1.56B parameter model has NOT yet occurred in this local environment because no CUDA GPU is physically present**.  
> In strict compliance with **Rule 1** (*No fake training, no fake safetensors, no simulation*), this audit certifies that:
> 1. No placeholder `.safetensors` files or random weights were fabricated.
> 2. `train.py` strictly refuses fake CPU training and halts with `TRAINING STATUS: BLOCKED (CUDA GPU REQUIRED)`.
> 3. An official Google Colab notebook (`train_qwen_colab.ipynb`) is provided for 1-click execution on a free T4/A100 GPU to produce the genuine ~35.2 MB LoRA adapter weights.
> 4. Legacy simulation files from the previous prototype run were isolated and archived in `legacy_simulation/`.

---

## 2. Mandatory Reality Questions & Evidence Matrix

| Audit Question | Audit Verdict | Empirical Evidence & Architectural Justification |
| :--- | :---: | :--- |
| **1. Is the base model real?** | **YES** | Loaded via `transformers.AutoConfig.from_pretrained('Qwen/Qwen2.5-1.5B-Instruct')`. Exact parameters verified: **1,562,179,072** across 28 transformer blocks ($d_{\text{model}}=1536$, $d_{\text{ffn}}=8960$, 12 attention heads, 2 KV heads). |
| **2. Is the tokenizer real?** | **YES** | Loaded via `transformers.AutoTokenizer.from_pretrained('Qwen/Qwen2.5-1.5B-Instruct')`. Exact vocabulary size verified: **151,665 tokens**. Official ChatML Jinja template verified. Truncation rate on 3,996 train notes: **0.00%** at sequence length 512. |
| **3. Is the LoRA adapter real?** | **YES (Architectural)** | Configured using `peft.LoraConfig(r=16, lora_alpha=32, target_modules=['q_proj', 'k_proj', 'v_proj', 'o_proj', 'gate_proj', 'up_proj', 'down_proj'])`. Exact trainable parameters: **18,464,768** (1.1820%). |
| **4. Are the adapter tensors real?** | **NO (Blocked)** | `adapters/real_qwen_lora/` contains no weights yet. Local environment lacks CUDA GPU. Per Rule 1, no placeholder `.safetensors` or random weights were created. Weights must be trained on GPU via `notebooks/train_qwen_colab.ipynb`. |
| **5. Was training actually executed?** | **NO (Blocked)** | Local CPU execution detected `torch.cuda.is_available() == False`. Fine-tuning 1.56B parameters on CPU is impractical (~40+ hours). `train.py` cleanly halted with exit code 2 and logged `TRAINING STATUS: BLOCKED`. |
| **6. Was CUDA used?** | **NO** | Local machine is Windows 10 AMD64 with `PyTorch 2.14.0+cpu`. CUDA is not available. GPU training is offloaded to the provided Google Colab notebook. |
| **7. Was generation actually autoregressive?** | **YES** | `inference.py` calls `model.generate()` using Hugging Face Transformers. Generates new token IDs step-by-step from the language model head. |
| **8. Are outputs generated by the model?** | **YES** | Output text originates directly from `tokenizer.decode(generated_tokens)`. Rules do NOT synthesize Risk, Key Finding, or Action. |
| **9. Are rules only safety/validation?** | **YES** | `prompts.py`'s `parse_clinical_output` strictly extracts fields generated by the model. If fields are missing or corrupted, it flags `is_valid_format = False` and routes to human review without overriding the model's output. |
| **10. Are metrics empirically measured?** | **YES** | All formulas in `evaluate.py` compute genuine scikit-learn metrics (`accuracy_score`, `f1_score(average='macro')`). No metrics are hardcoded. |
| **11. Are any simulations remaining?** | **NO** | Zero simulation logic remains in `src/`. All previous simulation artifacts and the 36-byte placeholder were quarantined in `legacy_simulation/`. Verified by `test_no_simulation_in_src`. |

---

## 3. Dataset Lineage & Integrity Certification

- **Source File**: `stage-4-slm/data-engineer/data/slm_finetune_dataset_v1.parquet`
- **File SHA-256**: `95d684c0940be3475375c69fc99a17f42b424d95ebc5a102cf608fa0889a1b2d`
- **Total Records**: 5,706
- **Record Distribution**:
  - `TRAIN`: 3,996 records (700 patients)
  - `VALIDATION`: 849 records (150 patients)
  - `TEST`: 861 records (150 patients)
- **Patient Isolation**: Exactly 1,000 unique patient IDs. **0 cross-split patient overlap** (verified by `test_cross_split_patient_isolation`).
- **Prompt-Target Leakage**: 0 duplicate notes or label leakage.

---

## 4. Token Length & Truncation Profile

Audited across all 3,996 training records using the official `Qwen2TokenizerFast`:

```text
Input Tokens (ChatML Prompt):
  Min:  156 tokens
  Max:  335 tokens
  Mean: 238.7 tokens
  P95:  320.0 tokens

Target Tokens (Assistant Response):
  Min:  51 tokens
  Max:  126 tokens
  Mean: 79.3 tokens
  P95:  108.0 tokens

Total Sequence Length:
  Min:  217 tokens
  Max:  453 tokens
  Mean: 318.0 tokens
  P95:  424.0 tokens

Truncation at max_seq_length = 512:  0 / 3,996 (0.00%)
Truncation at max_seq_length = 1024: 0 / 3,996 (0.00%)
```

**Conclusion**: At `max_seq_length = 512`, exactly **0.00%** of training sequences are truncated, preserving 100% of clinical entities, gene alterations, dosages, and adverse events.

---

## 5. PEFT LoRA Target Parameter Accounting

Audited against `Qwen2ForCausalLM` meta-tensor graph:

```text
Base Architecture:         Qwen2.5-1.5B-Instruct
Total Base Parameters:     1,562,179,072 (1.56B)
LoRA Rank (r):             16
LoRA Alpha:                32
LoRA Dropout:              0.05
LoRA Scaling Factor:       2.000
Target Projections (7):    q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj
Target Blocks:             28 transformer blocks (196 target matrices)
Trainable LoRA Parameters: 18,464,768 (~18.46M)
Trainable Percentage:      1.1820%
Expected Adapter File:     ~35.2 MB (FP16 / BF16 safetensors)
```

---

## 6. Execution Roadmap to Move from 'BLOCKED' to 'VERIFIED'

To transition Stage 5 from `NOT VERIFIED — REAL TRAINING REQUIRED` to `VERIFIED`:

```text
                  CURRENT STATE
     [Production Pipeline Built & Validated]
     [Local Training: BLOCKED (No CUDA GPU)]
                       │
                       ▼
    1. Open notebooks/train_qwen_colab.ipynb on Google Colab
    2. Attach free T4 or A100 GPU (16GB VRAM)
    3. Run 3 epochs of genuine LoRA fine-tuning (~45 minutes on T4)
    4. Save genuine adapter_model.safetensors (~35.2 MB)
    5. Download adapter to adapters/real_qwen_lora/
    6. Run reload test and empirical evaluation
                       │
                       ▼
               STAGE 5 VERIFIED
```

*Audit completed and certified by SLM Engineering Team.*
