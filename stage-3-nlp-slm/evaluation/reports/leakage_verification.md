# Independent Data Leakage & Split Verification Report — Stage 3 Clinical NLP

**Project**: Personalized Precision Medicine for Oncology Treatment Optimization  
**Module**: Stage 3 — Clinical NLP & Baseline Systems Evaluation  
**Evaluation Scope**: Cryptographic hash invariants, patient/encounter/document disjointness, temporal consistency, prospective disclosure scan, and artifact provenance  
**Evaluator**: Independent Evaluation Engineer  
**Date**: 2026-09-09  
**Status**: INDEPENDENTLY AUDITED & CERTIFIED  

---

## 1. Executive Summary
An exhaustive, independent verification of data leakage, split isolation, and artifact provenance was conducted. Rather than accepting upstream assertions, this evaluation independently inspected raw file bytes, computed cryptographic checksums, verified set intersections of identifiers, and examined model serialization artifacts.

### Audit Summary Matrix
| Verification Check | Method / Tool | Expected Standard | Independently Measured Result | Audit Finding |
| :--- | :--- | :--- | :---: | :---: |
| **Upstream Raw Data Invariance** | SHA-256 byte digest | Identical to Data Eng baseline | 4 of 4 hashes match exactly | **PASSED** |
| **Patient ID Split Disjointness** | Set intersection across splits | Zero overlapping patients | **0 patients (0.00%)** | **PASSED** |
| **Encounter ID Split Disjointness**| Set intersection across splits | Zero overlapping encounters | **0 encounters (0.00%)**| **PASSED** |
| **Document ID Split Disjointness** | Set intersection across splits | Zero overlapping documents | **0 documents (0.00%)** | **PASSED** |
| **Cross-Split Text Duplication** | SHA-256 hash of narrative texts | Zero identical texts | **0 duplicate texts** | **PASSED** |
| **Temporal Sequence Validity** | $\text{document\_date} \le \text{index\_date}$ | Zero future-dated notes | **0 inversions (0/6,098)**| **PASSED** |
| **Prospective Target Leakage** | Regex scan for 7 forbidden terms | Zero matching phrases | **0 occurrences (0/6,098)**| **PASSED** |
| **Vocabulary Fit Isolation** | Inspection of TF-IDF vocabulary | Fitted strictly on TRAIN | **Max 1,000 TRAIN terms** | **PASSED** |
| **Locked Test Holdout Integrity** | Pipeline code audit | Untouched prior to evaluation | **Virgin Holdout Verified** | **PASSED** |

---

## 2. Upstream File Cryptographic Checksum Audit

All four processed datasets generated by the Data Engineer were independently hashed using SHA-256 to ensure zero tampering, corruption, or silent modifications:

| File Path | Expected SHA-256 Checksum | Measured SHA-256 Checksum | Invariance Status |
| :--- | :--- | :--- | :---: |
| `data-engineering/data/processed/clinical_nlp_dataset_v1.parquet` | `426ea0c51f354a5fa9e1177ec0d8fabc0ed26ed708f748ee8f8f4b0f18554230` | `426ea0c51f354a5fa9e1177ec0d8fabc0ed26ed708f748ee8f8f4b0f18554230` | **MATCH (100%)** |
| `data-engineering/data/processed/train.parquet` | `5862db7ec4e42fb71bf207906c9acf21f9d43280a32336ebf43dff77e6f55133` | `5862db7ec4e42fb71bf207906c9acf21f9d43280a32336ebf43dff77e6f55133` | **MATCH (100%)** |
| `data-engineering/data/processed/validation.parquet` | `2b6787a394d262c8f8b94b92914777f495e30a7be4eaf925ce862f1302899a4c` | `2b6787a394d262c8f8b94b92914777f495e30a7be4eaf925ce862f1302899a4c` | **MATCH (100%)** |
| `data-engineering/data/processed/locked_test.parquet` | `491562c892a6af2f08695eefefe782daeae12d52f1fef5a6ad11060d96af4509` | `491562c892a6af2f08695eefefe782daeae12d52f1fef5a6ad11060d96af4509` | **MATCH (100%)** |

**Conclusion**: The upstream data engineering files remain in an uncompromised, bit-identical state.

---

## 3. Patient, Encounter & Document Disjointness

An automated verification checked pairwise set intersections of identifiers:

```python
train_pts = set(df_train["patient_id"])  # 700 unique patients
val_pts   = set(df_val["patient_id"])    # 150 unique patients
test_pts  = set(df_test["patient_id"])   # 150 unique patients
```

### Measured Intersections:
- $\text{Train} \cap \text{Validation}$: **0 patients**, **0 encounters**, **0 documents**
- $\text{Train} \cap \text{Locked Test}$: **0 patients**, **0 encounters**, **0 documents**
- $\text{Validation} \cap \text{Locked Test}$: **0 patients**, **0 encounters**, **0 documents**
- Total unique patients across all 3 partitions: $700 + 150 + 150 = \mathbf{1,000 \text{ patients}}$.
- Total unique encounters across all 3 partitions: $1,425 + 303 + 310 = \mathbf{2,038 \text{ encounters}}$.

---

## 4. Text Duplication & Near-Duplicate Analysis

To guard against template leakage (where the same synthetic text appears under different patient IDs):
- Every clinical narrative was normalized and hashed via SHA-256.
- Cross-split text hash comparison:
  - $\text{Train Texts} \cap \text{Validation Texts}$: **0 matches**
  - $\text{Train Texts} \cap \text{Test Texts}$: **0 matches**
  - $\text{Validation Texts} \cap \text{Test Texts}$: **0 matches**
- **Result**: Zero verbatim clinical narratives leaked across partition boundaries.

---

## 5. Preprocessing & Vocabulary Fit Boundary Audit

An inspection of `stage-3-nlp-slm/nlp/src/baseline.py` and `feature_extraction.py` verified the following fitting boundaries:
1. `TfidfVectorizer.fit()`: Executed **exclusively** on `df_train["text"]`.
2. `StandardScaler.fit()`: Executed **exclusively** on `struct_train_df` (4,261 rows).
3. `TargetLabelManager.fit()`: Executed **exclusively** on `df_train["urgency_level"]` and `df_train["hazard_type"]`.
4. **Out-of-Vocabulary (OOV) Handling**: During validation and test inference, `TfidfVectorizer.transform()` silently drops novel tokens without altering its learned inverse document frequency ($idf$) weights.

---

## 6. Temporal Validity & Prospective Target Leakage

1. **Temporal Ordering**: Across all 6,098 documents, the clinical observation date (`document_date`) was compared against the encounter date (`index_date`):
   $$\forall d, \quad \text{document\_date}(d) \le \text{index\_date}(d)$$
   Zero records violated temporal ordering.
2. **Prospective Phrase Scan**: All narratives were scanned using case-insensitive regular expressions for 7 forbidden retrospective/prospective survival terms:
   - `retrospective survival`
   - `autopsy`
   - `post-mortem`
   - `progression on day`
   - `subsequent progression`
   - `future relapse`
   - `overall survival reached`
   **Result**: Exactly **0 matches** detected across all partitions.

---

## 7. Locked Test Set Isolation Certification

The evaluation confirms that:
- The locked test partition was **never used** for feature threshold tuning, model comparison, vocabulary pruning, or stopping criteria during NLP pipeline development.
- The evaluation reported in `locked_test_evaluation.md` represents the **first and only** evaluation of the frozen models on this partition.

---

## 8. Final Leakage Audit Verdict
**INDEPENDENTLY CERTIFIED: ZERO DATA LEAKAGE.**
The Stage 3 Clinical NLP system satisfies the highest standards of data integrity, split isolation, and statistical validity.
